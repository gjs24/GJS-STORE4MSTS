# Portfolio

Static portfolio site for Jebas.

Open `index.html` directly in a browser to view it.

Edit these spots first:

- `index.html`: name, headline, about text, project cards, contact links.
- `styles.css`: colors, spacing, typography, and responsive layout.
- `assets/profile-mark.svg`: local hero visual.
