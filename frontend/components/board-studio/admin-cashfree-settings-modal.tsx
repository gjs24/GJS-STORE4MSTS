'use client';

// Deprecated: Cashfree gateway credentials are securely managed on the backend via Render environment variables.
// Manual client-side key entry has been removed.
export const AdminCashfreeSettingsModal = () => null;
